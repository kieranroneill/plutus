enum EnvironmentVariableKeyEnum {
  // base
  AppName = 'APP_NAME',
  AppPort = 'APP_PORT',
  AppVersion = 'APP_VERSION',
  LogLevel = 'LOG_LEVEL',
  NodeEnv = 'NODE_ENV',

  // database
  MongoDBPassword = 'MONGODB_PASSWORD',
  MongoDBPort = 'MONGODB_PORT',
  MongoDBUsername = 'MONGODB_USERNAME',
}

export default EnvironmentVariableKeyEnum;
